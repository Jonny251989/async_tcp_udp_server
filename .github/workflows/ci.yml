name: Async TCP/UDP Server CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Pull Docker image
        run: |
          docker pull jonnyrembo/async-tcp-udp-server:latest

      - name: Build project in container
        run: |
          docker run --rm \
            -v $(pwd):/workspace \
            jonnyrembo/async-tcp-udp-server:latest \
            bash -c "cd /workspace && make all"

      - name: Run all tests
        run: |
          # Используем существующий docker-compose.yml с сервисами test и systemd-test
          docker compose run --rm test
          docker compose run --rm systemd-test

      - name: Cleanup
        if: always()
        run: |
          docker compose down